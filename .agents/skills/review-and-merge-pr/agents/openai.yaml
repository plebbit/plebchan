interface:
  display_name: "Review and Merge PR"
  short_description: "Review PR feedback and act within the requested scope"
  default_prompt: "Use $review-and-merge-pr to assess the current PR and perform only the fixes or merge requested in this task."
